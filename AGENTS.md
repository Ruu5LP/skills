# このリポジトリのAgent運用ルール

このリポジトリは、実装そのものだけでなく、エンジニアリングプロジェクトの要望整理からリリース・振り返りまでを支援するSkillを管理する。

## 基本方針

- 大きな開発依頼は、いきなり実装せず、最初に目的・利用者・スコープ・非スコープ・完了条件を整理する。
- 仕様が曖昧な場合は推測で実装せず、不明点、仮説、判断が必要な事項を整理する。
- 作業は、レビュー・検証可能なGitHub Issueの大きさへ分割する。Epic、Feature、Task、必要に応じてEnablerやTestを区別する。
- 各タスクには、受入条件と確認方法を付ける。完了とはコードを書いた状態ではなく、受入条件を満たして確認された状態とする。
- 依存関係とブロッカーを明記し、外部待ち、社内判断待ち、仕様待ちを区別する。
- 遅延が発生してから報告するのではなく、遅延につながるリスク、兆候、影響、緩和策、担当者を事前に提示する。
- 本番リリースがある場合は、テスト、監視、段階展開、ロールバック、リリース後の確認まで計画する。
- 会議メモから、決定事項、未決事項、担当者、期限、次回確認事項を抽出する。

## 標準ワークフロー

1. 要望が曖昧なら `interview-me` で一問ずつ確認し、目的・制約・成功指標を明らかにする。
2. `spec-driven-development` で仕様、前提、境界、テスト方針を文書化する。
3. `planning-and-task-breakdown` で依存関係を確認し、縦切りのEpic・Feature・Taskへ分解する。
4. `github-to-tickets` でIssue化する。既存Issueの状態整理には `github-triage` を使う。
5. 優先順位の判断は `prioritization-advisor`、ブロッカーや改善項目の順位付けは `impediment-prioritization` を使う。
6. リリース前は `shipping-and-launch`、障害後は `incident-postmortem` を使う。

各Skillの出力は、このリポジトリの [PM運用ガイド](docs/pm-workflow.md) にあるIssue項目、受入条件、リスク、リリース項目に合わせる。

## 変更時の注意

- 既存の設定、Skill、テンプレートを削除・上書きしない。変更前に内容と差分を確認する。
- GitHub Issue、ラベル、担当者、Project項目など外部状態を変更する場合は、対象と内容を明示し、ユーザーの確認を得てから実行する。
- Skillを追加・更新するときは、Skill名、発行元、ライセンス、依存ファイル、Codexでの配置を確認し、同じ役割のSkillを重複させない。
- upstreamのSkill本文に補助Skillへの参照があっても、この構成に含まれないSkill名を推測して呼び出さない。必要になった場合は、存在・最新版・ライセンスを調査してから追加判断する。
- テストや確認を実行した場合は、コマンドと結果を報告する。未確認の受入条件を完了扱いにしない。
